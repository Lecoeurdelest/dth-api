version: "2"

syncs:
  dth-api-sync:
    src: './'
    sync_host_port: 10872
    sync_excludes: ['.git', '.idea', 'build', '.gradle', '*.iml', '*.log', 'node_modules', '.docker-sync']
    watch_excludes: ['build', '.gradle', '*.log']
    sync_strategy: 'unison'
